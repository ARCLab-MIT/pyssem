version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.8"

sphinx:
  configuration: docs/conf.py

python:
  version: "3.8"
  install:
    - method: pip
      path: .
    - requirements: requirements.txt

formats:
  - html
  - pdf

submodules:
  include: all

build:
  jobs: 2

environment:
  - READTHEDOCS: True

  jobs: 2

environment:
  - READTHEDOCS: True
